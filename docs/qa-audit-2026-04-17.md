# REDLINE FC Web - QA Audit (2026-04-17)

## Scope
- End-to-end QA/Fix pass against:
  - `docs/web-release-checklist.md`
  - `docs/Complete_Game_Development_Checklist.md`
  - `KNOWN_ISSUES.md`
- Includes direct fixes plus immediate regression checks.

## Automated Gate Results
| Check | Result | Evidence |
|---|---|---|
| `npm run test` | PASS | 19/19 tests passed |
| `npm run lint` | PASS | ESLint clean after Node globals config fix |
| `npm run build` | PASS | Vite production build successful |
| `npm run preview` smoke | PASS | HTTP 200 on `http://127.0.0.1:4173` |

## Gameplay/Rules Fixes Implemented
| Area | Change | Files |
|---|---|---|
| Overtime flow | Draw after 2nd half now routes into kickoff + overtime period | `src/game/Match.ts`, `src/game/Game.ts` |
| Sudden death flow | Draw after overtime now routes into sudden death kickoff + golden-goal ending | `src/game/Match.ts`, `src/game/Game.ts` |
| Penalty routing | Fouls in penalty area now route to a dedicated prototype penalty outcome (auto-goal + restart) | `src/game/Game.ts`, `src/game/Match.ts` |
| Tackle legality depth | Removed random foul/knockdown decision; now based on angle, late contact, and impact/reckless state | `src/game/Combat.ts` |
| Lint blocker | Added Node globals handling for `.js` files | `eslint.config.js` |

## Test Coverage Added
- Added/updated unit coverage in `src/game/Match.test.ts` for:
  - overtime transition
  - sudden death transition
  - penalty-goal registration

## Visual QA Notes (BrowserUse)
- BrowserUse setup-screen capture succeeded (overlay, captain select, lineup preview, controls panel visible).
- Additional headful evidence run completed with `/?autoboot=1&fast=1`:
  - in-play HUD capture
  - live goal/callout capture
  - scoreboard/possession progression capture
- Limitation in this environment:
  - reliable low-level keyboard injection for `Esc/Tab/WASD` is still constrained by available automation bridges,
    so pause-menu interaction and deterministic fulltime-overlay screenshot remain partially pending in-browser.

## Residual Risks / Still Open
- Set-piece restrictions (corner/throw-in dedicated flow) are still not fully implemented.
- Penalty handling exists as prototype routing, not yet a full dedicated player-controlled penalty sequence.
- Cross-browser + mobile matrix remains partially pending manual execution.
